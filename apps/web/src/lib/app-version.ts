/**
 * Published @ujima/agents version, inlined at Next.js build time from
 * packages/distribution/package.json (see apps/web/next.config.ts).
 */
export const APP_VERSION =
  process.env.NEXT_PUBLIC_UJIMA_VERSION?.trim() || "0.0.0-dev";
