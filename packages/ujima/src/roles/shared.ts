export const DEFAULT_ROLE_TOOLS = [
  "filesystem",
  "shell",
  "web_search",
  "message",
  "channel.post",
  "channel.reply",
  "channel.dm",
  "channel.list",
  "channel.read",
  "self.note",
  "mcp"
] as const;

export const DEFAULT_ROLE_CHANNELS = [
  "general"
] as const;
