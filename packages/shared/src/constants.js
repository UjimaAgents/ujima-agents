export const DEFAULT_GENERAL_CHANNEL = Object.freeze({
    id: "general",
    name: "general",
    kind: "general",
    topic: "",
    memberIds: [],
});
export const DEFAULT_WORKSPACE_ROLE_SCOPES = Object.freeze({});
